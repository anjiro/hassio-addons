# Changelog

## 1.0.0

- Initial release. Wraps `ghcr.io/ejagombar/fireflyiii-ai-categorizer:latest`
  directly, no local build. Configuration is done entirely through the app's
  own web UI (persisted to the addon's `/data` volume).
